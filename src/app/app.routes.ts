import { Routes } from '@angular/router';
import { SomethingInterestingComponent } from './test/test.component';

export const routes: Routes = [
    { path: "", component: SomethingInterestingComponent }
];
